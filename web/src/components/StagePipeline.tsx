// Deprecated: replaced by AgentFlow (components/AgentFlow.tsx).
// Kept as an empty module so stale imports fail loudly at build time.
export default function StagePipeline(_props: { stages: Record<string, string>; times: Record<string, number> }) {
  return null;
}
